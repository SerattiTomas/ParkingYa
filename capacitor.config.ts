import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'oemspot.parkingya',
  appName: 'ParkingYa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
